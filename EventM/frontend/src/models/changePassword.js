export default class ChangePassword {
    constructor(oldPassword, newPassword, repeatNewPassword) {
        this.oldPassword = oldPassword
        this.newPassword = newPassword
        this.repeatNewPassword = repeatNewPassword
    }
}